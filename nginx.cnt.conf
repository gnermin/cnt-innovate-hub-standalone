server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    # sav /api ide na CNT backend unutar docker mreže
    location /api/ {
        proxy_pass http://cnt-backend:8000;
    }
}
